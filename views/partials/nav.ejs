<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/y"><img class="navLogo my-auto" src="img/yPromptLogo3.png" alt="yPrompt"></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if(currentUser) { %> 
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/appts">Active Appts</a>
          </li> 
        </ul>
        <form class="my-auto" action="/logout" method="POST">
          <button class="btn btn-outline-dark" href="/logout">Logout</button>
        </form>
          <% } %>
      </ul>
        <% if(!currentUser) { %> 
          <a class="btn btn-outline-dark" href="/login">Login</a>
        <% } %> 
      </div>
    </div>
  </nav>